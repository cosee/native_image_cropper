name: native_image_cropper_ios_example
description: Demonstrates how to use the native_image_cropper_ios plugin.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  native_image_cropper_ios: ^0.3.0
  cupertino_icons: ^1.0.6
  # Saving cropped image to gallery.
  gal: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  cosee_lints: ^0.13.0
  dart_code_linter: ^4.0.0

flutter:
  assets:
    - assets/sail-boat.png
