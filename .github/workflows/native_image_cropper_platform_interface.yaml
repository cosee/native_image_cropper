name: native_image_cropper_platform_interface

on:
  push:
    paths:
      - 'native_image_cropper_platform_interface/**'
      - '.github/workflows/native_image_cropper_platform_interface.yaml'
  pull_request:
    paths:
      - 'native_image_cropper_platform_interface/**'
      - '.github/workflows/native_image_cropper_platform_interface.yaml'

jobs:
  build:
    defaults:
      run:
        working-directory: native_image_cropper_platform_interface
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2.10.0

      - name: Install Dependencies
        run: flutter pub get
      - name: Format
        run: dart format --set-exit-if-changed .
      - name: Analyze
        run: flutter analyze
      - name: Metrics
        run: dart run dart_code_metrics:metrics analyze . --set-exit-on-violation-level=warning

  pana:
    defaults:
      run:
        working-directory: native_image_cropper_platform_interface
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2.10.0

      - name: Install Dependencies
        run: |
          flutter pub get
          flutter pub global activate pana
      - name: Verify Pub Score
        run: ../tool/verify_pub_score.sh
