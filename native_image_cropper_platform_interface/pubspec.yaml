name: native_image_cropper_platform_interface
description: A common platform interface for the native_image_cropper plugin.
repository: https://github.com/cosee/native_image_cropper/tree/main/native_image_cropper_platform_interface
homepage: https://pub.dev/publishers/cosee.biz/packages
issue_tracker: https://github.com/cosee/native_image_cropper/issues
version: 0.1.2

environment:
  sdk: '>=2.18.4 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  cosee_lints: ^0.4.0
  dart_code_metrics: ^5.7.6
